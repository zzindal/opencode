#include<stdio.h>
main()
{
double num;
printf("Enter a number:");
scanf("%lf",&num);
if(num<=0.0)
{if(num==0.0)
printf ("entered nunber is zero");
else printf("entered no. is negetive");
}
else
printf("Entered number is positive");
}
