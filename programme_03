#include<stduio.h>
main()
{
int year;
printf("enter a year to check if it is a leap year\n");
scanf("%d"&year);
if(year%400==o)
printf("%d is a leap year %n",year);
else if(year%100==0)
printf("%d is'nt a leap year\n",year");
else if(year%==0)
printf("%d is a leap year\n",year);
else
printf("%D is'nt a leap year\n",year);
return(0);
 
